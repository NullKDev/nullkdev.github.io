---
import { renderIcon, type IconName } from '@/lib/icons'

interface Props {
  name: IconName
  size?: number
  class?: string
}

const { name, size = 20, class: className } = Astro.props
---

<span class:list={['icon', className]} set:html={renderIcon(name, size)} />
